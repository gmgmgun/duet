{
  "name": "duet",
  "version": "0.1.0",
  "description": "Duet — Claude(구현) × Codex(리뷰) 자동 페어 프로그래밍 로컬 서버",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build"
  },
  "engines": {
    "node": ">=18"
  }
}
